import { getDatabase } from '../lib/db';
import { v4 as uuidv4 } from 'uuid';

function seed() {
  // Script is disabled since database seeding is no longer applicable directly via this file
  // as the db requires async initialization now. Left here to pass TS compilation.
}

seed();
